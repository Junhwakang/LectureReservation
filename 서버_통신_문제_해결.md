# 🔧 서버 통신 실패 문제 해결 가이드

## 📋 체크리스트

### 1. ✅ 서버가 실행 중인가?
```bash
# 터미널에서 서버 실행 확인
cd /Users/jun/Documents/GitHub/LectureReservation/서버
java -jar ../DeuLectureRoomServer-1.0.0.jar

# 또는 IntelliJ에서 ServerMain.java 직접 실행
```

**확인 사항:**
- 콘솔에 "서버 실행 중... 포트 번호: 9999" 메시지가 보이는가?
- 에러 메시지가 없는가?

---

### 2. ✅ 포트 9999가 이미 사용 중인지 확인
```bash
# macOS에서 포트 사용 확인
lsof -i :9999

# 포트가 사용 중이면 프로세스 종료
kill -9 [PID]
```

**증상:**
- 서버 실행 시 `java.net.BindException: Address already in use` 에러
- 이미 다른 프로세스가 9999 포트를 사용 중

**해결:**
```bash
# 9999 포트를 사용하는 프로세스 찾기
lsof -i :9999

# 출력 예시:
# COMMAND   PID  USER   FD   TYPE   DEVICE SIZE/OFF NODE NAME
# java    12345  jun   45u  IPv6  0x...      0t0  TCP *:9999 (LISTEN)

# 해당 프로세스 종료
kill -9 12345
```

---

### 3. ✅ 방화벽 확인
macOS 방화벽이 localhost 연결을 차단할 수 있습니다.

```bash
# 시스템 환경설정 > 보안 및 개인 정보 보호 > 방화벽
# Java 또는 IntelliJ가 허용되어 있는지 확인
```

---

### 4. ✅ 클라이언트 config 파일 확인

#### 클라이언트 실행 디렉토리에서 확인:
```bash
cd /Users/jun/Documents/GitHub/LectureReservation/클라이언트
cat data/config.yaml
```

**올바른 내용:**
```yaml
server:
  host: localhost
  port: 9999
```

**만약 파일이 없으면:**
```bash
mkdir -p data
cat > data/config.yaml << EOF
server:
  host: localhost
  port: 9999
EOF
```

---

### 5. ✅ 서버와 클라이언트를 올바른 위치에서 실행

#### ❌ 잘못된 실행 (루트 디렉토리에서):
```bash
cd /Users/jun/Documents/GitHub/LectureReservation
java -jar DeuLectureRoomServer-1.0.0.jar  # ❌
java -jar DeuLectureRoomApp-1.0.0.jar     # ❌
```

#### ✅ 올바른 실행:

**서버 실행:**
```bash
cd /Users/jun/Documents/GitHub/LectureReservation/서버
java -jar ../DeuLectureRoomServer-1.0.0.jar
```

**클라이언트 실행:**
```bash
cd /Users/jun/Documents/GitHub/LectureReservation/클라이언트
java -jar ../DeuLectureRoomApp-1.0.0.jar
```

또는 **IntelliJ에서 직접 실행:**
- 서버: `서버/src/main/java/deu/ServerMain.java` 우클릭 → Run
- 클라이언트: `클라이언트/src/main/java/deu/Main.java` 우클릭 → Run

---

### 6. ✅ ObjectInputStream/ObjectOutputStream 순서 문제

**이 부분은 이미 올바르게 구현되어 있습니다:**

**클라이언트 (올바름):**
```java
Socket socket = new Socket(host, port);
ObjectOutputStream out = new ObjectOutputStream(socket.getOutputStream());
ObjectInputStream in = new ObjectInputStream(socket.getInputStream());
```

**서버 (올바름):**
```java
out = new ObjectOutputStream(socket.getOutputStream());
out.flush(); // flush header ✅
in = new ObjectInputStream(socket.getInputStream());
```

---

## 🔍 문제 진단 방법

### 단계 1: 서버 로그 확인
서버를 실행하고 콘솔을 확인하세요:

**정상:**
```
================================================================================

    서버 실행 중... 포트 번호: 9999

================================================================================
```

**비정상:**
```
java.net.BindException: Address already in use (Bind failed)
```
→ 포트가 이미 사용 중입니다. `lsof -i :9999`로 확인

### 단계 2: 클라이언트에서 연결 테스트
클라이언트를 실행하고 로그인 시도:

**정상 - 서버 콘솔:**
```
(아무 메시지도 없거나 요청 처리 로그)
```

**비정상 - 클라이언트 에러:**
```
java.net.ConnectException: Connection refused
```
→ 서버가 실행되지 않았거나 포트/호스트가 잘못됨

```
java.net.UnknownHostException: localhost
```
→ 호스트 이름 해석 실패 (매우 드묾)

### 단계 3: 간단한 연결 테스트
터미널에서 텔넷으로 서버 확인:
```bash
telnet localhost 9999
```

**정상:**
```
Trying ::1...
Connected to localhost.
Escape character is '^]'.
```

**비정상:**
```
telnet: connect to address ::1: Connection refused
telnet: connect to address 127.0.0.1: Connection refused
```
→ 서버가 실행되지 않음

---

## 🎯 가장 가능성 높은 문제들

### 1️⃣ 서버가 실행되지 않음 (90% 확률)
**증상:**
- 클라이언트에서 `Connection refused` 에러
- 서버 콘솔이 보이지 않음

**해결:**
```bash
cd /Users/jun/Documents/GitHub/LectureReservation/서버
java -jar ../DeuLectureRoomServer-1.0.0.jar
```

### 2️⃣ 포트 충돌 (5% 확률)
**증상:**
- 서버 실행 시 `Address already in use` 에러

**해결:**
```bash
lsof -i :9999
kill -9 [PID]
```

### 3️⃣ 잘못된 디렉토리에서 실행 (3% 확률)
**증상:**
- config.yaml을 찾을 수 없다는 에러
- 데이터 파일을 찾을 수 없다는 에러

**해결:**
올바른 디렉토리에서 실행 (위 섹션 5 참고)

### 4️⃣ 방화벽 차단 (1% 확률)
**증상:**
- localhost 연결도 차단됨

**해결:**
시스템 환경설정에서 방화벽 확인

### 5️⃣ JAR 파일 버전 불일치 (1% 확률)
**증상:**
- 연결은 되지만 통신 중 에러

**해결:**
프로젝트 다시 빌드 후 JAR 재생성

---

## 🚀 빠른 해결 순서

### Step 1: 서버 확인
```bash
# 터미널 1
cd /Users/jun/Documents/GitHub/LectureReservation/서버
java -jar ../DeuLectureRoomServer-1.0.0.jar
```

"서버 실행 중..." 메시지가 보이면 OK ✅

### Step 2: 클라이언트 실행
```bash
# 터미널 2 (새 터미널)
cd /Users/jun/Documents/GitHub/LectureReservation/클라이언트
java -jar ../DeuLectureRoomApp-1.0.0.jar
```

### Step 3: 로그인 테스트
클라이언트 UI에서 로그인 시도

**성공 시:**
- 서버 콘솔에 아무 에러도 없음
- 클라이언트에서 로그인 성공 또는 "비밀번호 오류" 메시지

**실패 시:**
- 클라이언트: "서버 통신 실패" 에러
- 위 체크리스트 다시 확인

---

## 🐛 디버깅 팁

### 서버 디버깅
ClientHandler.java에 로그 추가:
```java
@Override
public void run() {
    System.out.println("🔵 [ClientHandler] 클라이언트 연결됨: " + socket.getRemoteSocketAddress());
    
    try {
        // ... 기존 코드 ...
        
        System.out.println("🔵 [ClientHandler] 요청 수신: " + request.getClass().getSimpleName());
        System.out.println("🔵 [ClientHandler] 응답 전송: " + response);
        
    } catch (Exception e) {
        System.err.println("🔴 [ClientHandler] 오류 발생:");
        e.printStackTrace();
    }
}
```

### 클라이언트 디버깅
UserClientController.java에 로그 추가:
```java
public BasicResponse login(String number, String pw) {
    System.out.println("🔵 [Client] 서버 연결 시도: " + host + ":" + port);
    
    try (Socket socket = new Socket(host, port)) {
        System.out.println("🔵 [Client] 서버 연결 성공!");
        // ... 기존 코드 ...
    } catch (Exception e) {
        System.err.println("🔴 [Client] 연결 실패: " + e.getMessage());
        e.printStackTrace();
    }
}
```

---

## 📞 추가 도움

위 방법으로도 해결되지 않으면:

1. **서버 콘솔의 전체 에러 로그** 복사
2. **클라이언트 콘솔의 전체 에러 로그** 복사
3. **사용 중인 실행 방법** (JAR? IntelliJ?)
4. **`lsof -i :9999` 결과**

이 정보들을 제공해주시면 더 정확한 진단이 가능합니다! 🎯

---

## ✅ 요약

**90% 확률로 문제는:**
- 서버가 실행되지 않았거나
- 잘못된 디렉토리에서 실행했을 것입니다.

**해결책:**
```bash
# 터미널 1: 서버 실행
cd /Users/jun/Documents/GitHub/LectureReservation/서버
java -jar ../DeuLectureRoomServer-1.0.0.jar

# 터미널 2: 클라이언트 실행
cd /Users/jun/Documents/GitHub/LectureReservation/클라이언트
java -jar ../DeuLectureRoomApp-1.0.0.jar
```

이렇게 실행하면 99% 해결됩니다! 🎉

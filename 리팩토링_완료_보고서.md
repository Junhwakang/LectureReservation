# 강의실 예약 시스템 리팩토링 완료 보고서

## 📋 프로젝트 개요
- **프로젝트명**: 강의실 예약 시스템
- **리팩토링 목표**: 커맨드 패턴과 옵저버 패턴 적용
- **리팩토링 일자**: 2025년 11월 21일

---

## ✅ 요구사항 대응표

| 요구사항 ID | 내용 | 적용 패턴 | 구현 상태 |
|------------|------|----------|---------|
| SFR-401 | 클라이언트에서 관리자는 모든 예약 정보를 수정/삭제 | 커맨드 패턴 | ✅ 완료 |
| SFR-402 | 서버에서 변경되거나 삭제된 예약 정보 갱신 | 커맨드 패턴 | ✅ 완료 |
| SFR-403 | 관리자가 예약 신청을 승인/거부 | 커맨드 패턴 | ✅ 완료 |
| SFR-404 | 서버는 승인/거부 결과 저장 | 커맨드 패턴 | ✅ 완료 |
| SFR-405 | 관리자가 예약 취소 시 취소 원인 등록 | 커맨드 패턴 | ✅ 완료 |
| SFR-406 | 서버는 취소 원인 저장 | 커맨드 패턴 | ✅ 완료 |
| SFR-409 | 예약 승인/거부 시 사용자에게 알림 | 옵저버 패턴 | ✅ 완료 |
| SFR-410 | 관리자가 예약 취소 시 사용자에게 알림 | 옵저버 패턴 | ✅ 완료 |
| SFR-411 | 사용자가 자신의 예약 내역 조회/관리 | 기존 기능 | ✅ 완료 |
| SFR-412 | 서버는 예약 변경 이력 갱신 | 옵저버 패턴 | ✅ 완료 |

---

## 🎯 적용된 디자인 패턴

### 1. 커맨드 패턴 (Command Pattern)

#### 📂 구조
```
서버/src/main/java/deu/command/
├── ReservationCommand.java          (커맨드 인터페이스)
├── ReservationCommandInvoker.java   (커맨드 실행자)
├── ModifyReservationCommand.java    (예약 수정 커맨드)
├── DeleteReservationCommand.java    (예약 삭제 커맨드)
├── ApproveReservationCommand.java   (예약 승인 커맨드)
└── RejectReservationCommand.java    (예약 거부 커맨드)
```

#### 💡 주요 기능
- **예약 수정/삭제/승인/거부 작업을 커맨드 객체로 캡슐화**
- **Undo/Redo 기능 지원** - 실행 취소 및 재실행 가능
- **커맨드 히스토리 관리** - 모든 작업 이력 추적
- **단일 책임 원칙 준수** - 각 작업을 독립적인 커맨드로 분리

#### 📝 커맨드 종류

1. **ModifyReservationCommand** (SFR-401, SFR-402)
   - 예약 정보 수정
   - 원본 데이터 백업 (Undo용)
   - 옵저버에게 변경 알림

2. **DeleteReservationCommand** (SFR-405, SFR-406)
   - 예약 삭제 및 취소 원인 저장
   - 삭제된 예약 백업 (Undo용)
   - 옵저버에게 취소 알림

3. **ApproveReservationCommand** (SFR-403, SFR-404)
   - 예약 상태를 "승인"으로 변경
   - 이전 상태 백업 (Undo용)
   - 옵저버에게 승인 알림

4. **RejectReservationCommand** (SFR-403, SFR-404)
   - 예약 상태를 "거부"로 변경
   - 거부 사유 저장
   - 이전 상태 백업 (Undo용)
   - 옵저버에게 거부 알림

---

### 2. 옵저버 패턴 (Observer Pattern)

#### 📂 구조
```
서버/src/main/java/deu/observer/
├── ReservationObserver.java      (옵저버 인터페이스)
├── ReservationSubject.java       (Subject 클래스)
├── NotificationObserver.java     (사용자 알림 옵저버)
└── LoggingObserver.java          (로깅 옵저버)
```

#### 💡 주요 기능
- **예약 상태 변경 시 자동 알림 시스템**
- **다중 옵저버 지원** - 알림과 로깅을 동시에 처리
- **느슨한 결합** - Subject와 Observer 간 독립성 유지
- **파일 기반 알림** - 알림 데이터 영구 저장

#### 📝 옵저버 종류

1. **NotificationObserver** (SFR-409, SFR-410)
   - **사용자 알림**: 예약 승인/거부/취소 시 사용자에게 알림
   - **관리자 알림**: 새 예약 신청 시 관리자(admin)에게 알림
   - **파일 저장**: `/data/notifications/user_{학번}_notifications.txt`
   - **알림 내용**: 예약 ID, 건물/층/강의실, 날짜/시간, 제목, 상태, 사유

2. **LoggingObserver** (SFR-412)
   - **변경 이력 로깅**: 모든 예약 변경 내역 기록
   - **파일 저장**: `/data/logs/reservation_history.log`
   - **로그 형식**: `[시간] [작업] 예약 ID | 사용자 | 강의실 | 날짜/시간`

#### 🔔 알림 이벤트

| 이벤트 | 메서드 | 알림 대상 | 파일 위치 |
|--------|-------|---------|----------|
| 새 예약 신청 | `onReservationCreated()` | 관리자(admin) | `user_admin_notifications.txt` |
| 예약 승인 | `onReservationApproved()` | 신청한 사용자 | `user_{학번}_notifications.txt` |
| 예약 거부 | `onReservationRejected()` | 신청한 사용자 | `user_{학번}_notifications.txt` |
| 예약 취소 | `onReservationCancelled()` | 신청한 사용자 | `user_{학번}_notifications.txt` |
| 예약 수정 | `onReservationModified()` | 신청한 사용자 | `user_{학번}_notifications.txt` |

---

## 📁 수정된 파일 목록

### 서버 (Server)

#### 1. Command 패키지 (새로 생성)
- ✨ `deu/command/ReservationCommand.java` - 커맨드 인터페이스
- ✨ `deu/command/ReservationCommandInvoker.java` - 커맨드 실행자
- ✨ `deu/command/ModifyReservationCommand.java` - 예약 수정
- ✨ `deu/command/DeleteReservationCommand.java` - 예약 삭제
- ✨ `deu/command/ApproveReservationCommand.java` - 예약 승인
- ✨ `deu/command/RejectReservationCommand.java` - 예약 거부

#### 2. Observer 패키지 (새로 생성)
- ✨ `deu/observer/ReservationObserver.java` - 옵저버 인터페이스
  - 새로 추가: `onReservationCreated()` 메서드
- ✨ `deu/observer/ReservationSubject.java` - Subject 클래스
  - 새로 추가: `notifyReservationCreated()` 메서드
- ✨ `deu/observer/NotificationObserver.java` - 사용자 알림
  - 새로 추가: `onReservationCreated()` 구현 (관리자 알림)
- ✨ `deu/observer/LoggingObserver.java` - 로깅
  - 새로 추가: `onReservationCreated()` 구현 (로그 기록)

#### 3. Controller 패키지
- 🔧 `deu/controller/business/ReservationManagementController.java`
  - 커맨드 패턴 적용
  - 옵저버 등록 및 관리
  - 새 메서드: `handleApproveRoomReservation()`, `handleRejectRoomReservation()`
  - Undo/Redo 기능 추가

- 🔧 `deu/controller/business/ReservationController.java`
  - 새 예약 생성 시 옵저버 알림 추가
  - `ReservationSubject` 통합

- 🔧 `deu/controller/SystemController.java`
  - 새 커맨드 핸들러 추가: "예약 승인", "예약 거부"
  - 취소 원인 지원 업데이트

#### 4. Service 패키지
- 🔧 `deu/service/ReservationService.java`
  - `lastCreatedReservation` 필드 추가
  - `getLastCreatedReservation()` 메서드 추가

#### 5. Model 패키지
- 🔧 `deu/model/entity/RoomReservation.java`
  - 새 필드 추가: `cancellationReason` (취소 원인)
  - 새 필드 추가: `rejectionReason` (거부 원인)

### 클라이언트 (Client)

#### 1. Controller 패키지
- 🔧 `deu/controller/business/RoomReservationManagementClientController.java`
  - 새 메서드: `approveRoomReservation()` - 예약 승인
  - 새 메서드: `rejectRoomReservation()` - 예약 거부 + 사유
  - 오버로드: `deleteRoomReservation()` - 취소 원인 지원

#### 2. Event Controller
- ✅ `deu/controller/event/ReservationManagementSwingController.java`
  - 이미 거부/취소 사유 입력 UI 구현됨
  - `JOptionPane`으로 사유 입력받음

---

## 🗂️ 데이터 저장 구조

### 알림 파일
```
서버/data/notifications/
├── user_admin_notifications.txt      # 관리자 알림 (새 예약 신청)
├── user_20210001_notifications.txt   # 학생 알림 (승인/거부/취소/수정)
└── user_20210002_notifications.txt   # 학생 알림
```

### 로그 파일
```
서버/data/logs/
└── reservation_history.log           # 모든 예약 변경 이력
```

### 예약 데이터
```
서버/data/
└── reservations.yaml                 # 예약 정보 (취소/거부 사유 포함)
```

---

## 🎨 클래스 다이어그램

### 커맨드 패턴
```
<<interface>>
ReservationCommand
  + execute(): BasicResponse
  + undo(): BasicResponse
  + getDescription(): String
         ↑
         |
    ┌────┴────┬────────┬───────────┐
    |         |        |           |
Modify    Delete   Approve     Reject
Command   Command  Command    Command

ReservationCommandInvoker
  - commandHistory: Stack<ReservationCommand>
  - undoStack: Stack<ReservationCommand>
  + executeCommand()
  + undo()
  + redo()
```

### 옵저버 패턴
```
<<interface>>
ReservationObserver
  + onReservationApproved()
  + onReservationRejected()
  + onReservationCancelled()
  + onReservationModified()
  + onReservationCreated()
         ↑
         |
    ┌────┴────┐
    |         |
Notification  Logging
Observer      Observer

ReservationSubject
  - observers: List<ReservationObserver>
  + attach(observer)
  + detach(observer)
  + notifyReservationApproved()
  + notifyReservationRejected()
  + notifyReservationCancelled()
  + notifyReservationModified()
  + notifyReservationCreated()
```

---

## 🔄 작업 흐름도

### 1. 예약 승인 시나리오 (SFR-403, SFR-404, SFR-409)
```
[관리자 UI]
    ↓
[ReservationManagementSwingController]
    ↓
[RoomReservationManagementClientController.approveRoomReservation()]
    ↓ (네트워크)
[ServerMain] → [SystemController]
    ↓
[ReservationManagementController.handleApproveRoomReservation()]
    ↓
[ApproveReservationCommand.execute()]
    ├─→ 예약 상태 변경 (ReservationRepository)
    └─→ [ReservationSubject.notifyReservationApproved()]
            ├─→ NotificationObserver: 사용자에게 알림 파일 생성
            └─→ LoggingObserver: 로그 파일 기록
```

### 2. 예약 거부 시나리오 (SFR-403, SFR-404, SFR-409)
```
[관리자 UI] - 거부 사유 입력
    ↓
[ReservationManagementSwingController]
    ↓
[RoomReservationManagementClientController.rejectRoomReservation(id, reason)]
    ↓ (네트워크)
[ServerMain] → [SystemController]
    ↓
[ReservationManagementController.handleRejectRoomReservation()]
    ↓
[RejectReservationCommand.execute()]
    ├─→ 예약 상태 변경 + 거부 사유 저장 (ReservationRepository)
    └─→ [ReservationSubject.notifyReservationRejected()]
            ├─→ NotificationObserver: 사용자에게 거부 알림 (사유 포함)
            └─→ LoggingObserver: 로그 파일 기록
```

### 3. 예약 취소 시나리오 (SFR-405, SFR-406, SFR-410)
```
[관리자 UI] - 취소 사유 입력
    ↓
[ReservationManagementSwingController]
    ↓
[RoomReservationManagementClientController.deleteRoomReservation(id, reason)]
    ↓ (네트워크)
[ServerMain] → [SystemController]
    ↓
[ReservationManagementController.handleDeleteRoomReservation()]
    ↓
[DeleteReservationCommand.execute()]
    ├─→ 예약 삭제 + 취소 사유 저장 (ReservationRepository)
    └─→ [ReservationSubject.notifyReservationCancelled()]
            ├─→ NotificationObserver: 사용자에게 취소 알림 (사유 포함)
            └─→ LoggingObserver: 로그 파일 기록
```

### 4. 새 예약 신청 시나리오 (관리자 알림)
```
[사용자 UI] - 예약 신청
    ↓
[ReservationSwingController]
    ↓
[RoomReservationClientController.addRoomReservation()]
    ↓ (네트워크)
[ServerMain] → [SystemController]
    ↓
[ReservationController.handleAddRoomReservation()]
    ↓
[ReservationService.createRoomReservation()]
    ├─→ 예약 저장 (ReservationRepository)
    └─→ lastCreatedReservation 저장
    ↓
[ReservationSubject.notifyReservationCreated()]
    ├─→ NotificationObserver: 관리자(admin)에게 알림
    └─→ LoggingObserver: 로그 파일 기록
```

---

## 🧪 테스트 시나리오

### 1. 커맨드 패턴 테스트
- ✅ 예약 수정 → Undo → 원본 복원 확인
- ✅ 예약 삭제 → Undo → 예약 복원 확인
- ✅ 예약 승인 → Undo → 대기 상태로 복원 확인
- ✅ 여러 커맨드 실행 → 히스토리 확인
- ✅ Undo → Redo → 재실행 확인

### 2. 옵저버 패턴 테스트
- ✅ 새 예약 신청 → `/data/notifications/user_admin_notifications.txt` 생성 확인
- ✅ 예약 승인 → 사용자 알림 파일 생성 확인
- ✅ 예약 거부 + 사유 → 알림에 거부 사유 포함 확인
- ✅ 예약 취소 + 사유 → 알림에 취소 사유 포함 확인
- ✅ 모든 작업 → `/data/logs/reservation_history.log` 기록 확인

### 3. 통합 테스트
- ✅ 관리자 예약 승인 → 사용자 알림 + 로그 기록
- ✅ 관리자 예약 거부 → 사용자 알림 + 로그 기록 + 거부 사유 저장
- ✅ 관리자 예약 삭제 → 사용자 알림 + 로그 기록 + 취소 사유 저장
- ✅ 사용자 예약 신청 → 관리자 알림 + 로그 기록

---

## 🎯 핵심 개선 사항

### 1. 코드 품질
- ✅ **단일 책임 원칙 (SRP)** 준수
  - 각 커맨드 클래스가 하나의 작업만 담당
  - 각 옵저버가 하나의 책임만 수행 (알림 OR 로깅)

- ✅ **개방-폐쇄 원칙 (OCP)** 준수
  - 새로운 커맨드 추가 시 기존 코드 수정 불필요
  - 새로운 옵저버 추가 시 Subject 코드 수정 불필요

- ✅ **의존성 역전 원칙 (DIP)** 준수
  - 추상화(인터페이스)에 의존
  - 구체 클래스 간 직접 의존 제거

### 2. 유지보수성
- ✅ **명확한 패턴 구조** - 새 개발자도 쉽게 이해
- ✅ **확장 용이** - 새 커맨드/옵저버 추가 간단
- ✅ **테스트 용이** - 각 컴포넌트 독립적으로 테스트 가능

### 3. 기능성
- ✅ **Undo/Redo 지원** - 작업 실행 취소 및 재실행
- ✅ **자동 알림 시스템** - 상태 변경 시 자동으로 알림
- ✅ **변경 이력 추적** - 모든 작업 로그 기록
- ✅ **다중 알림 대상** - 사용자 + 관리자 알림

---

## 📝 추가 권장 사항

### 1. 향후 개선 가능 항목
- [ ] 실시간 알림 시스템 (WebSocket 활용)
- [ ] 이메일/SMS 알림 통합
- [ ] 예약 변경 이력 UI 구현
- [ ] 알림 읽음 상태 관리
- [ ] 알림 설정 (ON/OFF)

### 2. 성능 최적화
- [ ] 알림 파일 크기 관리 (주기적 정리)
- [ ] 로그 파일 로테이션 (일별/월별)
- [ ] 옵저버 비동기 처리 (Thread Pool)

### 3. 보안 강화
- [ ] 알림 파일 암호화
- [ ] 접근 권한 검증 강화
- [ ] 로그 데이터 민감 정보 마스킹

---

## ✨ 결론

이번 리팩토링을 통해:
1. **커맨드 패턴**을 적용하여 예약 관리 작업을 캡슐화하고 Undo/Redo 기능을 구현했습니다.
2. **옵저버 패턴**을 적용하여 예약 상태 변경 시 자동 알림 시스템을 구축했습니다.
3. **모든 요구사항 (SFR-401 ~ SFR-412)** 을 충족했습니다.
4. **코드 품질과 유지보수성**이 크게 향상되었습니다.
5. **확장 가능한 구조**로 설계하여 향후 기능 추가가 용이합니다.

---

## 📞 문의사항
- 담당자: 강준화
- 패턴 적용: 커맨드 패턴 + 옵저버 패턴
- 완료 일자: 2025-11-21

---

**본 시스템은 디자인 패턴을 올바르게 적용하여 유지보수가 용이하고 확장 가능한 구조로 리팩토링되었습니다.** ✅
